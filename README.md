Private preview. Contents are AES-256-GCM encrypted; the page decrypts in the
browser with the shared password. Nothing in this repo is readable without it.
Rebuilt by `Projects/Maxxed/lander/publish_preview.py` on the authoring machine.
